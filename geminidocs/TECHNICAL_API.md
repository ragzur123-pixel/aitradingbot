# AiTradingBot: Technical API Reference (Extended)

## 🧠 Strategic Decision Engine (`5_trading_bot.py`)
Multi-model reasoning loop with Monte Carlo verification.
- `run_trading_bot(ticker)`: Executes Discovery, Audit, and Synthesis phases.
- `Monte Carlo Consensus`: Requires 4/5 confluence across temperature-shifted iterations.
- `Local Frozen Veto`: Final check via Llama 3.1 8B to prevent model drift.

## 🏢 Fund Orchestration & Risk
- `master_orchestrator.py`:
    - `get_risk_scaler(ticker)`: Tiered gate logic for connectivity/news.
    - `get_portfolio_exposure()`: Aggregate sector/currency monitor.
- `risk_manager.py`:
    - `DynamicRegimeClassifier`: Adjusts volatility-floor stops based on market state.
    - `validate_trade(trade_params)`: Final math-gate before execution.
- `allocator.py`:
    - `MultiStrategyAllocator`: Distributes USD risk based on weekly evolution weights.

## 🌉 Execution Bridges
- `ibkr_bridge.py` (**DMA Path**):
    - `execute_dma_order(symbol, side, qty, limit_price)`: Uses randomized Tactical Offsets to evade HFT.
    - `apply_randomized_offset(price, side)`: Jitters limit price by 0.01-0.05%.
- `autonomous_pipeline.py` (**Clockwork**):
    - `run_operation(ticker)`: Orchestrates snapshots and bot iterations in a non-blocking loop.

## 🔬 Research & Alpha Intelligence
- `research_historian.py`:
    - `log_shadow_trade(trade_data)`: Records every 'Paper' trade with 10bps slippage penalty.
    - `log_regime_interpretation(...)`: Maps macro context to AI reasoning for future pruning.
- `pairs_trading_scanner.py`:
    - `find_best_index_basis_pair(tickers)`: Detects divergence between assets and Sector ETFs.
- `sentiment_sentinel.py`:
    - `HarvardConsensusEngine`: Multi-source sentiment validation and trimmed-mean calculation.
- `raw_data_processor.py`:
    - `prepare_macro_tensor(macro_dict)`: Converts numeric data into high-fidelity context strings.

## 📐 Mathematical Foundation
- `indicators.py`:
    - `Wilder's Smoothing`: RSI and ATR implementation for professional-grade stability.
    - `OU Process`: Calculates mean-reversion speed for Stat-Arb.
- `geometry.py`:
    - `get_geometric_anchors(df)`: Euclid-confirmed swing points and trend geometry.
    - `detect_liquidity_sweeps(df)`: Identifies stop-runs using wick-rejection delta.

## 🛠️ Operational Safety
- `HeartbeatMonitor`: Systemic watchdog halting on API/DB/AI failure.
- `Dead-Man's Switch`: VPS fail-safe to close positions if home heartbeats stop.
- `ConnectivitySentinel`: Ping/Jitter auditor (< 350ms enforcement).
- `AccountReconciler`: Cycle-by-cycle Broker-to-DB synchronization.

## 💼 Capital & Career Integration (`hustle_fund_manager.py`)
Manages the 'Treasury Fund' and tracks Prop Firm readiness.
- `get_fund_dashboard()`: Provides a bird's-eye view of career income vs. bot performance.
- `get_prop_firm_readiness()`: Audits performance against FTMO/Topstep criteria.
- `HustleFundManager`: Handles the allocation of career capital into Index Funds vs. Bot risk.

## 📢 Unified Communication Hub (`notifier.py`)
Telegram and Discord integration with trade approval gates.
- `notify(message)`: Broadcasts high-priority alerts across configured channels.
- `wait_for_approval(trade_details)`: Blocks execution until a Human-In-The-Loop provides manual confirmation.

## 🧠 Meta-Learning & Failure Audit (`strategy_auditor.py`)
Post-mortem engine that analyzes trade failures to detect systemic biases.
- `generate_systemic_bias_report()`: Identifies if failures are due to 'Chop', 'Stop Hunts', or 'Drift'.
- `StrategyAuditor`: Dynamically updates the `last_valid_bias.json` to steer the Alpha engines.

## 📚 Strategy Knowledge Ingestion (`1_download_youtube.py`)
RAG-based ingestion for building the AI's 'Strategy Soul'.
- `run_ingestion()`: Processes YouTube transcripts into a ChromaDB vector store.
- `YoutubeLoader`: Specialized interface for capturing high-fidelity strategic narratives.

---
*Generated by Gemini CLI - 2026-06-01*
